# Supabase — copy to .env.local and fill in. Never commit real values.

# Safe to expose to the browser.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server only. Bypasses RLS — never prefix with NEXT_PUBLIC_ and never import
# into a client component.
SUPABASE_SERVICE_ROLE_KEY=
